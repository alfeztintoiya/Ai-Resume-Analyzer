# ===========================================
# EMAIL SERVICE CONFIGURATION GUIDE
# ===========================================

# Add these to your .env file in the Backend directory

# Email Service Settings (Required for email verification)
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-gmail-app-password

# Optional: Custom email settings
EMAIL_FROM_NAME=Resume Analyzer
EMAIL_FROM_ADDRESS=noreply@resumeanalyzer.com

# Application URLs (Required for email links)
FRONTEND_URL=http://localhost:5173
BACKEND_URL=http://localhost:5003

# ===========================================
# HOW TO GET GMAIL APP PASSWORD
# ===========================================

# 1. Go to Google Account settings: https://myaccount.google.com/
# 2. Security → 2-Step Verification (must be enabled)
# 3. App passwords → Generate password for "Resume Analyzer"
# 4. Copy the 16-character password (no spaces)
# 5. Use this password in EMAIL_PASSWORD (not your regular Gmail password)

# ===========================================
# TEST EMAIL CONFIGURATION
# ===========================================

# After adding credentials, test with:
# node test-email.js

# ===========================================
# SECURITY NOTES
# ===========================================

# - Never commit real credentials to git
# - Use app-specific passwords, not account passwords  
# - Consider using environment-specific .env files
# - For production, use proper email service providers
